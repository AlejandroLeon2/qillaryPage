---
interface Props {
  label: string;
  href?: string;
  children?: Array<{
    label: string;
    href: string;
    day?: string;
  }>;
}

const { label, href, children = [] } = Astro.props;
const hasChildren = children.length > 0;
---

<li class="poppins bg-white">
  {
    hasChildren ? (
      <div class="group border-b transition-all duration-500 ease-in-out border-gray-200  md:border-transparent  ">
        <p class="flex  font-bold escuchar relative group   items-center justify-between md px-2 py-2 cursor-pointer  text-black hover:text-qillary-aqua ">
          {label}

          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class=" transition-all text-gray-600 duration-300 ease-in-out group-hover:rotate-90 group-focus:rotate-90 group-active:rotate-90"
          >
            <path d="m9 18 6-6-6-6" />
          </svg>
        </p>

        <ul class=" md:absolute md:left-0 rounded-sm  h-auto max-h-100  md:right-0 md:mx-6 lg:mx-20 overflow-y-scroll  md:bg-white hidden  md:gap-4 group-focus:grid group-hover:grid lg:grid-cols-2  md:px-4 md:py-4">
          {children.map((subItem) => (
            <li class="pl-8 md:pl-0 ">
              <a
                title={`${subItem.label} - Full tour in ${subItem.day}`}
                href={subItem.href}
                class="menu-link text-xs xl:text-sm  py-2 flex  hover:text-cyan-600"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 384 512"
                  class="w-4 h-4 mr-1 "
                  fill="currentColor"
                >
                  <path d="M192 0C86 0 0 86 0 192c0 77.4 27.5 99.6 172.3 310.6a24 24 0 0 0 39.4 0C356.5 291.6 384 269.4 384 192 384 86 298 0 192 0zm0 272a80 80 0 1 1 80-80 80 80 0 0 1-80 80z" />
                </svg>
                <span class="text-xs xl:text-sm text-black">
                  {subItem.label}
                </span>
              </a>
            </li>
          ))}
        </ul>
      </div>
    ) : (
      <a
        title={label}
        href={href}
        class="menu-link  border-b border-gray-200 md:border-white font-bold flex items-center justify-between px-3 py-2 text-black hover:text-qillary-aqua "
      >
        {label}
      </a>
    )
  }
</li>
