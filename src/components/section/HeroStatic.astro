---
const {
  link,
  titulo = "Slow Down",
  sub = "Let yourself be carried by the journey",
  descripcion = "  Explore diverse destinations with confidence and ease—our team takes careof every detail so you can focus on making lasting memories.",
} = Astro.props;
---

<section
  style={`background-image: url('${link}');`}
  class={`h-[90vh] md:h-[90vh] pt-100 md:mt-15 bg-fixed relative bg-cover bg-center`}
>
  <div
    class="text-center text-white absolute top-1/3 h-20 right-0 left-0 mx-2 py-4"
  >
    <p class="satisfy text-2xl md:text-4xl">{sub}</p>
    <p class="text-5xl md:text-6xl font-semibold">{titulo}</p>
    <p class="max-w-lg mx-auto px-6 md:px-0 mt-4">{descripcion}</p>
  </div>
</section>
